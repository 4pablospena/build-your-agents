{
  "name": "build-your-agents",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm run build:dash",
    "build:dash": "node scripts/sync-templates.mjs && node scripts/build-zip.mjs && node scripts/generate-og.mjs && nuxt build",
    "dev": "nuxt prepare && nuxt dev",
    "generate": "node scripts/sync-templates.mjs && node scripts/build-zip.mjs && node scripts/generate-og.mjs && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare && node scripts/sync-templates.mjs && node scripts/build-zip.mjs && node scripts/generate-og.mjs"
  },
  "devDependencies": {
    "@nuxt/content": "2",
    "@nuxtjs/tailwindcss": "^6.14.0",
    "@tailwindcss/typography": "^0.5.19",
    "autoprefixer": "^10.5.0",
    "nuxt": "^3.13.0",
    "postcss": "^8.5.14",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.4.5",
    "vue-tsc": "^2.0.21"
  },
  "dependencies": {
    "sharp": "^0.34.5",
    "vue": "^3.4.27",
    "vue-router": "^4.3.2"
  }
}
